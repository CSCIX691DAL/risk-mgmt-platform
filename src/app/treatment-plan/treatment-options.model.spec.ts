import { TreatmentOptions } from './treatment-options.model';

describe('TreatmentOptions', () => {
  it('should create an instance', () => {
    expect(new TreatmentOptions()).toBeTruthy();
  });
});
